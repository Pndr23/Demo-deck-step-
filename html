<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Deck Step DEMO</title>
  <link rel="stylesheet" href="style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet" />
</head>
<body>
  <!-- Controlli in alto -->
  <div class="top-controls" style="position: absolute; top: 10px; right: 10px; display: flex; gap: 8px; z-index: 1000;">
    <button id="historyButton" class="icon-button" title="Cronologia" aria-label="Cronologia">🕒</button>
    <div id="soundToggle" class="icon-button"
         style="width:40px; height:40px; display:flex; align-items:center;
                justify-content:center; font-size:24px; border-radius:50%;
                background:#333; color:#fff; cursor:pointer;">
      🔊
    </div>
  </div>

  <!-- Titolo -->
  <h1 id="gameTitle">Deck Step DEMO</h1>

  <!-- Pulsante start -->
  <div id="setupArea" style="text-align:center; margin-top:20px;">
    <button id="startButton">🎮 Inizia DEMO</button>
  </div>

  <!-- Area di gioco -->
  <div id="gameArea" class="hidden">
    <div id="scorePanel">
      <span id="correctLabel">✅ Corrette: <span id="correctCount">0</span></span>
      <span id="errorLabel" style="margin-left:20px;">❌ Errori: <span id="errorCount">0</span></span>
      <span id="jollyLabel" style="margin-left:20px;">🃏 Jolly: <span id="jollyCount">0</span></span>
    </div>

    <div id="cardsContainer" style="display:flex; justify-content:center; gap:20px; margin-top:20px;">
      <div>
        <p>Carta attuale:</p>
        <img id="currentCardImg" src="cards/card_back.png" alt="Carta attuale" />
      </div>
      <div>
        <p>Carta pescata:</p>
        <img id="drawnCardImg" src="cards/card_back.png" alt="Carta pescata" />
      </div>
    </div>

    <h2 id="challengeText" style="margin-top:20px;">Sfida in corso...</h2>
    <div id="challengeButtons" style="margin-top:10px;"></div>
  </div>

  <script src="script.js"></script>
</body>
</html>
